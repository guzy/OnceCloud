package com.beyondsphere.dao;

import java.util.Date;

public interface LogBackupDAO {
	
	/**
	 * 将超过设置时限的日志转移到备份表中
	 * @author lining
	 * @param savetime
	 * @return
	 */
	public abstract boolean insertLogBackup(Date savetime);
}
